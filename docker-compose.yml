services:
  traefik:
    image: traefik:v3.3
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./traefik.yml:/traefik.yml:ro
      - ./certs:/certs:ro
      - ./dynamic:/dynamic:ro
    networks:
      - proxy

networks:
  proxy:
    external: true
